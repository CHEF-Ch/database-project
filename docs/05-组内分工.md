# 组内分工与贡献

> 2—3 人一组，共用一个仓库。分工要真实，每位成员都应能解释完整项目，而非只解释自己负责的文件。

## 成员

> Git 身份一栏用于对照 `git log` 的提交证据；**同一人可能有两个名字**（真实姓名与 Git 配置名不同），在此一并记录以免误认为两人。

| 成员 | Git 身份 | 负责内容 | 实际贡献与提交证据 |
| --- | --- | --- | --- |
| Chen | `CHEF-Ch <15392812510@163.com>` | 业务流程与数据边界、设计约定、表清单、交叉复核 | 第 1 周全部（`77ae344`→`f920f24`）：经营场景与边界、角色与流程、业务规则、进库/不进库清单、AI 使用记录。第二周 ①②（`cd00628`）：设计约定 13 条、17 张表清单与切表判据、24 条关联、枚举取值清单、5 条字段级决定。第二周 ⑥（`bc01e81`）：交叉复核，修正 `employee_id` 可空性与订单明细粒度措辞 |
| Yang | `Rosa <2679838366@qq.com>` | 字段定义、码标注、样例元组 | 第二周 ③④⑤（`287defc`）：17 张表字段字典、主码/候选码/外码标注、`sql/02-seed.sql`（238 行、7 张订单覆盖全部状态与支付组合）、`docs/02` §1.1 枚举取值补齐、§5 口径说明（类型选择、候选码口径、流水符号约定）；进度与缺口（`f4fa431`）；AI 使用记录（`43d0745`） |
| zeng | `Dale835 <2753046099@qq.com>` | 参与表结构讨论 | `17ef365` 提交 `sql/week2/zeng-week2.sql`（一张自拟的 `product` 建表草案）；`a35107c` 自行撤回该文件，未合入最终表结构 |

> **贡献说明**：第 1 周由 Chen 主导；第 2 周的 ① ②（模式骨架）由 Chen 完成，③④⑤（字段定义与样例）由 Yang 完成，⑥ 由 Chen 复核——**步骤 ⑥ 特意由他人执行**，因复核的意义在"换一双眼睛"，自己查自己等于没查。
> 第 2 周各步的完成者与状态见 [`docs/06-进度/第二周任务流程与状态.md`](06-进度/第二周任务流程与状态.md)。

## 协作约定

- 代码仓库地址：<https://github.com/CHEF-Ch/database-project>
- 提交规范：提交信息写明"第 X 周 + 步骤号 + 做了什么"；**每位成员用各自的 Git 身份提交**，便于 `git log` 追溯贡献。
- 分支策略：**当前在 `main` 上直接提交**。已在第 2 周出现过一次分叉（两人各自基于同一提交推进，后推者需先 `pull --rebase`）——推动前先 `git pull --rebase origin main`，有冲突当次解决。若后续多人同时改同一份文档（如 `docs/02`）导致冲突频繁，再引入 `feature/*` 分支。
- **跨人复核**：涉及表结构、业务规则等关键产出的修改，由**另一名成员**复核后再提交或回改，复核结论记入 `docs/04-AI使用记录/`。
