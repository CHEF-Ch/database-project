# 第二周：关系模式与数据字典

> 把业务对象转成关系模式：表清单、字段定义（属性 + 域）、码标注、样例元组。

## 1. 设计约定

- 金额、数量、时间等通用类型约定（如金额 decimal(10,2)、时间 datetime2(0)）
- 命名规范（表名、字段名、主码/外码命名）
- PK = 主码，UK = 唯一候选码，FK = 外码

## 2. 关系模式与属性域

> 每张表列出：字段（类型、是否可空、默认值）、主码、候选码、外码、CHECK 约束。

### （表名）

| 字段 | 类型 / 域 | 约束 | 说明 |
| --- | --- | --- | --- |
| | | | |

- 主码：  
- 候选码：  
- 外码：  

## 3. 主要关联

- 谁对谁是一对多 / 多对多 / 一对一（例如商品与原料通过配方形成多对多）
- 删除规则（默认不级联删除，被引用的商品应下架而非删除）

## 4. 样例元组与预期结果

> 真实可执行的样例数据（放 `sql/02-seed.sql`），并能说清每行的经营含义。

| 对象 | 样例 | 含义 |
| --- | --- | --- |
| | | |

## 5. 字段 / 键的口径说明

- 每个字段"为什么是这个类型、这个长度"
- 为什么某个键是候选码 / 为什么不是（答辩常问）
